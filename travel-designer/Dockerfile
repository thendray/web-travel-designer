# Используйте официальный образ Node.js в качестве базового
FROM node:16

# Установить рабочую директорию
WORKDIR /app

# Копировать package.json и package-lock.json для установки зависимостей
COPY package*.json ./

# Установить все зависимости приложения
RUN npm install

# Копировать все файлы приложения в docker-контейнер
COPY . .

# Собрать приложение (возможно, для Vue требуется отдельная команда сборки, проверьте документацию вашего приложения)
RUN npm run build

# Указать порт, который приложение будет слушать
EXPOSE 3000

# Запустить приложение
CMD ["npm", "start"]